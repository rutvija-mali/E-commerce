package com.demo.order;

import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.HttpServlet;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;

import java.io.BufferedReader;
import java.io.IOException;
import java.util.List;

import org.json.JSONObject;
@WebServlet("/updateOrderServlet")
public class updateOrderServlet extends HttpServlet {

	protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {
		
		StringBuilder sb = new StringBuilder();
		BufferedReader reader = request.getReader();
		String line;
		while ((line = reader.readLine()) != null) {
			sb.append(line);
		}
		reader.close();
		
		JSONObject data = new JSONObject(sb.toString());
		
		String payment_id = data.getString("paymentId");
		String order_id = data.getString("orderId");
		String status = data.getString("status");
		
		int checkUpdate = OrderDao.update(payment_id, order_id, status);
		if(checkUpdate > 0) {
			System.out.println("Data order update successfuly");
			List<Cart> list = 
			
			
			
		}else {
			System.out.println("something went wrong in updating iorder");
		}
		
		
		
	}

}
